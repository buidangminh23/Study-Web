{
  "title": "SQL SELECT and Filtering",
  "summary": "Write SQL SELECT queries to retrieve, filter, and sort data from database tables using WHERE, AND, OR, LIKE, and ORDER BY.",
  "widget_type": "default",
  "position": 1,
  "is_published": true,
  "content_html": "<h2>What Is SQL?</h2><p>SQL (Structured Query Language) is the standard language for querying and managing relational databases. While MS Access uses a visual Query Design Grid, it generates SQL behind the scenes. Understanding SQL directly is essential for professional database work.</p><h2>Basic SELECT Syntax</h2><pre><code>SELECT column1, column2, ...\nFROM   table_name\nWHERE  condition\nORDER BY column [ASC | DESC];\n\n-- Select all columns:\nSELECT * FROM Movie;\n\n-- Select specific columns:\nSELECT title, ratingCode FROM Movie;\n\n-- Select with alias:\nSELECT title AS MovieTitle, relyear AS Year FROM Movie;</code></pre><h2>WHERE Clause and Comparisons</h2><pre><code>-- Exact match:\nSELECT title, ratingCode FROM Movie WHERE relyear = 2015;\n\n-- Comparison operators:\n=   equal\n<>  not equal\n>   greater than\n<   less than\n>=  greater than or equal\n<=  less than or equal\n\n-- Text match (case-insensitive in Access):\nSELECT title FROM Movie WHERE ratingCode = 'PG';</code></pre><h2>AND and OR Operators</h2><pre><code>-- AND: both conditions must be true\nSELECT title FROM Movie\nWHERE relyear = 2013 AND ratingCode = 'M';\n\n-- OR: at least one condition must be true\nSELECT title FROM Movie\nWHERE relyear = 2013 OR relyear = 2016;\n\n-- Watch out: AND on the same field is often a mistake\nSELECT title FROM Movie\nWHERE relyear = 2013 AND relyear = 2016;  -- returns 0 rows!</code></pre><h2>LIKE and Wildcards</h2><pre><code>-- Access wildcards:\n*  = any string of any length    (SQL standard uses %)\n?  = exactly one character       (SQL standard uses _)\n\n-- Titles starting with THE:\nSELECT title FROM Movie WHERE title LIKE 'the *';\n\n-- Titles containing 'day':\nSELECT title FROM Movie WHERE title LIKE '*day*';\n\n-- In standard SQL:\nSELECT title FROM Movie WHERE title LIKE 'The %';\nSELECT title FROM Movie WHERE title LIKE '%day%';</code></pre><h2>ORDER BY and BETWEEN</h2><pre><code>-- Sort alphabetically by title:\nSELECT title, relyear FROM Movie ORDER BY title ASC;\n\n-- Sort by year descending:\nSELECT title, relyear FROM Movie ORDER BY relyear DESC;\n\n-- Range filter using BETWEEN:\nSELECT title FROM Movie\nWHERE relyear BETWEEN 2011 AND 2015;\n-- Equivalent to: relyear >= 2011 AND relyear <= 2015</code></pre><h2>Aggregate Functions</h2><pre><code>COUNT(*) - number of rows\nSUM()    - total of a numeric column\nAVG()    - average value\nMIN()    - minimum value\nMAX()    - maximum value\n\n-- Count movies per rating:\nSELECT ratingCode, COUNT(*) AS Total\nFROM   Movie\nGROUP BY ratingCode;\n\n-- Average runtime per year (rounded to 1 decimal):\nSELECT relyear, ROUND(AVG(runtime), 1) AS AvgRuntime\nFROM   Movie\nGROUP BY relyear\nHAVING AVG(runtime) >= 100;</code></pre>",
  "exercises": [
    {
      "title": "SELECT all",
      "prompt": "Which SQL statement retrieves all columns from the Movie table?",
      "exercise_type": "multiple_choice",
      "options": [
        "SELECT * FROM Movie",
        "GET ALL FROM Movie",
        "SELECT Movie.*",
        "SHOW Movie"
      ],
      "answer": "SELECT * FROM Movie"
    },
    {
      "title": "WHERE filter",
      "prompt": "Which query returns all movies released in 2015?",
      "exercise_type": "multiple_choice",
      "options": [
        "SELECT * FROM Movie WHERE relyear = 2015",
        "SELECT * FROM Movie HAVING relyear = 2015",
        "SELECT * FROM Movie ORDER BY relyear = 2015",
        "FILTER Movie WHERE relyear = 2015"
      ],
      "answer": "SELECT * FROM Movie WHERE relyear = 2015"
    },
    {
      "title": "AND vs OR",
      "prompt": "SELECT * FROM Movie WHERE relyear = 2013 AND relyear = 2016 — how many rows does this return?",
      "exercise_type": "multiple_choice",
      "options": [
        "0 rows — a movie cannot be released in both years",
        "All rows from 2013 and 2016",
        "Only 2013 movies",
        "Only 2016 movies"
      ],
      "answer": "0 rows — a movie cannot be released in both years"
    },
    {
      "title": "LIKE wildcard",
      "prompt": "Which query finds all movies whose title contains 'day'?",
      "exercise_type": "multiple_choice",
      "options": [
        "SELECT title FROM Movie WHERE title LIKE '*day*'",
        "SELECT title FROM Movie WHERE title = 'day'",
        "SELECT title FROM Movie WHERE title CONTAINS 'day'",
        "SELECT title FROM Movie WHERE title LIKE 'day'"
      ],
      "answer": "SELECT title FROM Movie WHERE title LIKE '*day*'"
    },
    {
      "title": "ORDER BY",
      "prompt": "To sort movies from newest to oldest, which clause is used?",
      "exercise_type": "multiple_choice",
      "options": [
        "ORDER BY relyear DESC",
        "ORDER BY relyear ASC",
        "GROUP BY relyear DESC",
        "SORT BY relyear DESCENDING"
      ],
      "answer": "ORDER BY relyear DESC"
    },
    {
      "title": "COUNT aggregate",
      "prompt": "Which aggregate function counts the number of rows in a group?",
      "exercise_type": "multiple_choice",
      "options": [
        "COUNT(*)",
        "SUM(*)",
        "TOTAL(*)",
        "NUMBER(*)"
      ],
      "answer": "COUNT(*)"
    },
    {
      "title": "GROUP BY",
      "prompt": "The GROUP BY clause is used together with:",
      "exercise_type": "multiple_choice",
      "options": [
        "Aggregate functions like COUNT, SUM, AVG",
        "Only the WHERE clause",
        "ORDER BY exclusively",
        "The LIKE operator"
      ],
      "answer": "Aggregate functions like COUNT, SUM, AVG"
    },
    {
      "title": "HAVING",
      "prompt": "What is the difference between WHERE and HAVING?",
      "exercise_type": "multiple_choice",
      "options": [
        "WHERE filters rows before grouping; HAVING filters groups after aggregation",
        "They are identical in function",
        "HAVING filters rows; WHERE filters columns",
        "WHERE is used with JOINs; HAVING is used alone"
      ],
      "answer": "WHERE filters rows before grouping; HAVING filters groups after aggregation"
    }
  ]
}